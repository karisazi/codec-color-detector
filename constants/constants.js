const detect_color_option = {
    realtime: "REALTIME",
    document: "DOCUMENT"
}

const camera_type = {
    back: "BACK",
    front: "FRONT"
}

const flash_mode = {
    off: "OFF",
    on: "ON"
}

export default {
    detect_color_option,
    camera_type,
    flash_mode
}